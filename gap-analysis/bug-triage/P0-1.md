# P0-1: Fix NODE_ENV in Dockerfile

```yaml
- id: P0-1
  title: "Fix NODE_ENV in Dockerfile"
  file: app/utils/clientLogger.ts
  line: 177
  root_cause: >
    The code relies on the `NODE_ENV` environment variable to configure
    production-safe defaults for logging and sampling. If `NODE_ENV` is not
    correctly set in the Dockerfile, the application may default to development
    settings, leading to incorrect behavior in production environments.
  fix_complexity: Simple
  requires_spec: false
  requires_test: false
  fix_summary: "Ensure the Dockerfile sets NODE_ENV to 'production' for production builds."
  antipattern: none
```
