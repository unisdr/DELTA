# P0-10: Fix type export bugs in humanCategoryPresence + hipHazard

```yaml
- id: P0-10
  title: "Fix type export bugs in humanCategoryPresence + hipHazard"
  file: app/backend.server/models/disaster_record.ts
  line: 28
  root_cause: >
    The `DisasterRecordsFields` interface is defined as an `Omit` of `SelectDisasterRecords` (line 28),
    but `SelectDisasterRecords` is imported from `hipHazardTable` (line 5), which may not correctly
    represent the structure of the `humanCategoryPresenceTable` or other related tables. This can lead
    to type mismatches or missing fields when interacting with these tables.
  fix_complexity: Simple
  requires_spec: false
  requires_test: true
  fix_summary: "Ensure type definitions for `DisasterRecordsFields` and related exports align with the actual table schemas."
  antipattern: none
```
