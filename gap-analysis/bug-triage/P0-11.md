# P0-11: Remove dead `countryName` column from instance_system_settings

```yaml
- id: P0-11
  title: "Remove dead `countryName` column from instance_system_settings"
  file: app/backend.server/models/user/invite.ts
  line: 31
  root_cause: >
    The `countryName` parameter is passed to multiple functions, such as `sendInviteForNewUser` and `sendInviteForExistingUser`, and is used in email templates. However, the column `countryName` in the `instance_system_settings` table is no longer in use, making this parameter redundant. Its continued presence may lead to confusion or errors if the column is removed from the database schema.
  fix_complexity: Simple
  requires_spec: false
  requires_test: true
  fix_summary: "Remove the unused `countryName` parameter and its references in the codebase."
  antipattern: none
```
