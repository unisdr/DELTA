# P0-12: Remove secret logging in env.ts

```yaml
- id: P0-12
  title: "Remove secret logging in env.ts"
  file: app/utils/logger.server.ts
  line: 124
  root_cause: >
    The `console.log` statement at line 124 logs the remote logging API key
    (`options.apiKey`) in plaintext, which is a security risk. Secrets should
    never be logged in plaintext as it can lead to unauthorized access if logs
    are exposed.
  fix_complexity: Simple
  requires_spec: false
  requires_test: false
  fix_summary: "Remove or mask the API key in the `console.log` statement."
  antipattern: AP-09
```
