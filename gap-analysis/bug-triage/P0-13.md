# P0-13: Fix `rejectUnauthorized: false` in SMTP transport

```yaml
- id: P0-13
  title: "TLS disabled in SMTP transport configuration"
  file: app/backend.server/models/event.ts
  line: 223
  root_cause: >
    The code includes a configuration where `rejectUnauthorized` is set to `false` in the SMTP transport setup. This disables TLS certificate validation, which can expose the system to man-in-the-middle attacks by allowing untrusted certificates.
  fix_complexity: Simple
  requires_spec: false
  requires_test: true
  fix_summary: "Enable TLS certificate validation by setting `rejectUnauthorized` to `true` or removing the option entirely."
  antipattern: AP-10
```
