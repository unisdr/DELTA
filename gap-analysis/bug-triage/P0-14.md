# P0-14: Fix `sanitizeInput` — remove destructive quote stripping

```yaml
- id: P0-14
  title: "Destructive quote stripping in sanitizeInput"
  file: app/utils/security.ts
  line: 17
  root_cause: >
    The `sanitizeInput` function removes single quotes, double quotes, and semicolons from input strings (line 17). This is a destructive operation that can alter valid user input, potentially leading to data corruption or unexpected behavior. The function is intended to prevent SQL injection but does so in a way that is overly aggressive and not context-aware.
  fix_complexity: Simple
  requires_spec: true
  requires_test: true
  fix_summary: "Remove the destructive quote stripping logic from sanitizeInput."
  antipattern: AP-11
```
