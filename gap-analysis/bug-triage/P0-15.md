# P0-15: Fix `destroyUserSession` graceful handling of missing session

```yaml
- id: P0-15
  title: "destroyUserSession does not handle missing session gracefully"
  file: app/utils/session.ts
  line: 47
  root_cause: >
    The `destroyUserSession` function is expected to be idempotent, meaning it should not throw an error if the session does not exist. However, the current implementation does not handle the case where the session is already absent, leading to unhandled exceptions during logout after session expiry.
  fix_complexity: Simple
  requires_spec: false
  requires_test: true
  fix_summary: "Add a check to ensure `destroyUserSession` returns successfully when the session does not exist."
  antipattern: none
```
