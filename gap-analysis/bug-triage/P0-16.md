# P0-16: Delete dead cost calculation API endpoints (4 files, zero callers)

```yaml
- id: P0-16
  title: "Dead cost calculation API endpoints with no callers"
  file: app/backend.server/models/analytics/hazard-analysis.ts
  line: 1245
  root_cause: >
    The function `getTotalDamagesByHazardFilters` is implemented to calculate total damages
    based on hazard filters, but there is no evidence of it being called anywhere in the codebase.
    This indicates that the function is unused and may be a candidate for removal.
  fix_complexity: Simple
  requires_spec: false
  requires_test: false
  fix_summary: "Remove the unused `getTotalDamagesByHazardFilters` function."
  antipattern: none
```
