# P0-17: Fix `export_tables_for_translation.ts` — writes to directory not file

```yaml
- id: P0-17
  title: "Writes to directory instead of file in export_tables_for_translation.ts"
  file: app/backend.server/models/event.ts
  line: 1804
  root_cause: >
    The `processAndSaveAttachments` function takes a `directory` parameter and uses it to construct a save path for attachments. However, the function does not ensure that the constructed path points to a file rather than a directory, leading to potential misbehavior when saving attachments.
  fix_complexity: Simple
  requires_spec: false
  requires_test: true
  fix_summary: "Ensure the save path explicitly targets a file rather than a directory."
  antipattern: none
```
