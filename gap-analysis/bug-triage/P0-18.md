# P0-18: Add `.dockerignore` — prevent image bloat and secret leak

```yaml
- id: P0-18
  title: "Add `.dockerignore` — prevent image bloat and secret leak"
  file: app/backend.server/models/user/totp.ts
  line: 48
  root_cause: >
    The `totpSecret` and `totpSecretUrl` values, which contain sensitive information, are being stored in the database. Without a `.dockerignore` file, there is a risk that these secrets or other sensitive files could be unintentionally included in Docker images, leading to potential security vulnerabilities.
  fix_complexity: Simple
  requires_spec: false
  requires_test: false
  fix_summary: "Add a `.dockerignore` file to exclude sensitive files and unnecessary data from Docker images."
  antipattern: AP-08
```
