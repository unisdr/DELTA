# P0-19: Fix `build_binary.sh` — build failure must be fatal

```yaml
- id: P0-19
  title: "Build failure in `build_binary.sh` is not treated as fatal"
  file: app/backend.server/services/mcp/handlers.ts
  line: 47
  root_cause: >
    The script `build_binary.sh` does not properly handle build failures as fatal errors. This could lead to scenarios where subsequent processes are executed despite the build not completing successfully, potentially causing undefined behavior or errors downstream.
  fix_complexity: Simple
  requires_spec: false
  requires_test: true
  fix_summary: "Ensure that the script exits immediately with a non-zero status code upon build failure."
  antipattern: none
```
