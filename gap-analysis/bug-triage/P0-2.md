# P0-2: Fix no-op `deleteById` await

```yaml
- id: P0-2
  title: "Fix no-op `deleteById` await"
  file: app/backend.server/models/human_effects_test.ts
  line: 113
  root_cause: >
    The `resetTestData` function calls `createTestDisasterRecord1(dr)` on line 113 without awaiting its result.
    This can lead to a race condition where subsequent test setup or assertions may execute before the data is fully created.
  fix_complexity: Simple
  requires_spec: false
  requires_test: true
  fix_summary: "Add `await` to the `createTestDisasterRecord1(dr)` call to ensure proper sequencing."
  antipattern: AP-01
```
