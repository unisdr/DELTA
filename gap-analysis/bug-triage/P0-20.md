# P0-20: Add CSP header to `entry.server.tsx` — missing from production

```yaml
- id: P0-20
  title: "Add CSP header to `entry.server.tsx` — missing from production"
  file: app/routes/$lang+/analytics+/disaster-events.tsx
  line: 29
  root_cause: >
    The Content Security Policy (CSP) header, which is critical for mitigating certain types of attacks like XSS, is not being set in the `entry.server.tsx` file. This omission leaves the application vulnerable to potential security risks in production environments.
  fix_complexity: Simple
  requires_spec: false
  requires_test: true
  fix_summary: "Add a CSP header configuration to `entry.server.tsx` to enforce security policies."
  antipattern: none
```
