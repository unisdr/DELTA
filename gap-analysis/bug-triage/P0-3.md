# P0-3: Remove debug console.log + lint rule

```yaml
- id: P0-3
  title: "Remove debug console.log + lint rule"
  file: app/components/ContentRepeater/index.tsx
  line: 156
  root_cause: >
    The code contains a `console.log` statement at line 156, which is used for debugging purposes to log a message when Leaflet and Leaflet.draw are loaded successfully. This is an anti-pattern (AP-04) as it introduces unnecessary console output in production code, which can clutter logs and potentially expose sensitive information.
  fix_complexity: Trivial
  requires_spec: false
  requires_test: false
  fix_summary: "Remove the `console.log` statement at line 156."
  antipattern: AP-04
```
