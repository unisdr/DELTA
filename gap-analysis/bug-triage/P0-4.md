# P0-4: Add coverage thresholds baseline

```yaml
- id: P0-4
  title: "Coverage thresholds not set to current baseline"
  file: vitest.config.ts
  line: 30
  root_cause: >
    The coverage configuration in the test setup does not specify a threshold
    for code coverage. This omission means there is no mechanism to enforce
    a baseline coverage level, which could lead to unintentional regression
    in test coverage over time.
  fix_complexity: Simple
  requires_spec: false
  requires_test: false
  fix_summary: "Set the coverage threshold to the current passing coverage level."
  antipattern: none
```
