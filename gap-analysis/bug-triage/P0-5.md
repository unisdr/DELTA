# P0-5: Fix placeholder support email in ErrorBoundary

```yaml
- id: P0-5
  title: "Placeholder support email in ErrorBoundary"
  file: app/routes/$lang+/user+/forgot-password.tsx
  line: 12
  root_cause: >
    The `sendForgotPasswordEmail` function is used to send emails, but there is no evidence in the provided code that the support email is being sourced from an environment variable. This suggests that a placeholder email address might be hardcoded, which is not configurable for different deployments as intended.
  fix_complexity: Simple
  requires_spec: false
  requires_test: true
  fix_summary: "Replace the placeholder support email with a value sourced from an environment variable."
  antipattern: none
```
