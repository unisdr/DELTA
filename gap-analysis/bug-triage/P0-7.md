# P0-7: Fix `deleteAllData` silent error swallow

```yaml
- id: P0-7
  title: "Silent error swallowing in deleteAllDataByDisasterRecordId"
  file: app/routes/$lang+/api+/disaster-record+/delete.$id.ts
  line: 48
  root_cause: >
    The `deleteAllDataByDisasterRecordId` function is invoked within a `.catch` block
    (line 48) to handle errors during the deletion process. However, the error is
    converted into a generic response object without re-throwing or logging the error
    details. This results in silent error swallowing, making it difficult to debug
    or trace the root cause of failures.
  fix_complexity: Simple
  requires_spec: false
  requires_test: true
  fix_summary: "Ensure errors are logged or re-thrown instead of being silently converted."
  antipattern: AP-12
```
