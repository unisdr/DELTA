# P0-8: Fix `revokeUserApiAccess` sets emailVerified=false

```yaml
- id: P0-8
  title: "`revokeUserApiAccess` sets emailVerified=false"
  file: app/backend.server/models/event.ts
  line: 265
  root_cause: >
    The `revokeUserApiAccess` function appears to set the `emailVerified` field to `false` when revoking API access for a user. This behavior is incorrect as revoking API access should not alter the `emailVerified` status, which is unrelated to API access permissions.
  fix_complexity: Simple
  requires_spec: false
  requires_test: true
  fix_summary: "Ensure that `revokeUserApiAccess` does not modify the `emailVerified` field when revoking API access."
  antipattern: none
```
